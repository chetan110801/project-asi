# 25 · Applied AI & agentic systems
**How intelligence gets turned into real systems that do useful work — the enduring principles, not the frameworks.**  *(Trunk · tier: Core)*

`Domain landing page · reading order lives in ../00_MAP.md · grounding index: ../../RESOURCES/corpus/_ATLAS.md`

**Scope (the durable "applied" layer that stands on `20-machine-intelligence/`):**
- **Agentic systems** — planning loops, tool-use, memory, orchestration; *why* long-horizon reliability degrades (errors compound) and the patterns that fight it.
- **Retrieval & grounding** — RAG and knowledge grounding as the answer to "the model doesn't know your world / hallucinates."
- **Evaluation of AI systems** — how you actually tell if an AI system works (evals, judges, failure analysis) — the skill that separates real builders from demo-makers.
- **AI system & product design** — the design patterns for putting a model inside something reliable; human-AI interaction; where to keep a human in the loop.
- **AI-for-X archetypes** — the *reusable shapes* of applying AI to a domain (science, code, medicine…), linked to the concrete cases in the `70-sciences/` + `80-engineering/` branches.

> **The AI-proof line ([`../../INSTRUCTIONS/HARD_RULES.md`](../../INSTRUCTIONS/HARD_RULES.md) §2.5):** this shelf teaches the **principles and patterns** you rely on to *direct and judge* the building of AI systems — **not** LangChain/CrewAI/etc. API syntax (the AI writes that). Grounded in the session-17 agentic-engineering corpus (`frontier-rnd/`: agent-framework docs + the anti-"vibe-coding" methodology canon) + OpenAI/Anthropic agent guides + the papers `D9-agents-rag`.

**Modules here:** ⬜ *(planned — an applied-AI cluster, slotted on the spine after the machine-intelligence core `1300` and its infrastructure `1400`; see [`../_QUEUE.md`](../_QUEUE.md)).*

> Stands on `20-machine-intelligence/` (what the models are) and feeds the `BUILDS/` track (BUILD_SYSTEM.md) — this shelf is the *understanding* of how to apply; the build track is the *doing*.
