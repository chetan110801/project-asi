# CONCEPT REGISTRY — one home per concept
**The no-redundancy enforcer.** Before explaining *anything*, check here. If a concept is listed, **link to its home — don't re-explain**. If it's not, explain it once in the right module, then add it here.

`Status: Living index · System Version: 3.0 · Last updated: 2026-07-18 (⭐ DEEP-DIVE CAMPAIGN continues — group ⑤ now has 12 modules: the AP8 trilogy + AP4 #1 (the engine) + AP5 #1 (learning without a teacher) + AP9 #1 (the open-ended engine) + AP1 #1 (the anatomy of a scaling law) + AP3 #1 (inside the agent loop) + AP2 #1 (how a machine thinks longer) + AP6 #1 (predictive coding & the learning rule) + AP7 #1 (the join, opened up) + **AP10 #1 (inside the robot brain)**; 6 new `c-robot-brain-vla` rows registered — the representation fork (discrete action tokens vs. continuous diffusion/flow-matching), the forced fast-slow split (System 2 knower + System 1 doer + a thin latent wire), action chunking, the data pyramid (web meaning · pooled logs + sim · teleoperated skill; co-training), the sim-to-real patch (domain randomization), and the mechanism verdict (the body trains the controller, not the concepts). Map complete — 11 approach cards + the verdict + BOTH cross-cutting pages)`

> Status legend: ✅ = home exists · 🔜 = home planned (queued in [`_QUEUE.md`](_QUEUE.md)/[CURRICULUM](CURRICULUM.md)); a 🔜 concept was *seeded* (briefly introduced, not fully explained) at the module noted, and its full pre-split explanation survives in that module's git history.

> Rule (from [`../INSTRUCTIONS/LEARNING_ARCHITECTURE.md`](../INSTRUCTIONS/LEARNING_ARCHITECTURE.md) §5): every concept has exactly **one canonical module**, which lives in one **domain folder**. This table maps **concept → home module (`id` + folder) → one-line pointer**. It does *not* hold the explanation itself (that would be repetition) — it points to it. (Folders for planned modules: see [`00_MAP.md`](00_MAP.md).)

---

## ★ The visible ladder (v3.0) — link THESE from new pages

> These are the **only pages visible in the reader** — the new "approaches to AGI" learning ladder, in order (① base → ② the approaches → ③ across → ④ verdict), followed by the optional **⑤ deep-dives** that branch off individual cards (home ids like `c-measure-of-intelligence`, marked *"deep dive ⑤"* below). When a new page needs one of these ideas, give a one-line refresher and link its home here. **Everything in the big "legacy" table below now lives in `LEARNING/_legacy/` (hidden 2026-07-14) — kept as a record; do NOT link legacy files from visible pages.**

| Concept | Home (`id` → file) | One-line pointer |
|---|---|---|
| language model / guessing the next word | `c-next-word` → [next-word](10-how-ai-works-today/01_guessing-the-next-word.md) | Guesses the next word, giving odds to every word; writes by looping. |
| self-supervision (data teaches itself) | `c-next-word` → [next-word](10-how-ai-works-today/01_guessing-the-next-word.md) | The next word is its own answer key → the whole internet can teach it. |
| prediction ≈ compression | `c-next-word` → [next-word](10-how-ai-works-today/01_guessing-the-next-word.md) | No room to memorise → forced to keep patterns; some say that ≈ understanding. |
| autoregressive (guess, add, repeat) | `c-next-word` → [next-word](10-how-ai-works-today/01_guessing-the-next-word.md) | Writes one word at a time; its own words feed back in. |
| the "mistake-count" (loss) | `c-next-word` → [next-word](10-how-ai-works-today/01_guessing-the-next-word.md) intro; used in [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md) | How wrong the next-word guess is; training drives it down. |
| scaling laws (the steady line) | `c-scaling-laws` → [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md) | Bigger → mistakes fall in a steady, predictable line. |
| compute-optimal / Chinchilla | `c-scaling-laws` → [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md) | For a set budget, a *right shape* — grow size & text together (now running-cost aware). |
| the data wall | `c-scaling-laws` → [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md) | Good human text is finite — "we have but one internet." |
| emergent abilities + the "mirage" | `c-scaling-laws` → [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md) | Skills that seem to switch on at size — real, or a scoring trick? (open) |
| the "make it bigger" bet / scale hypothesis | `c-ap1-scale` → [AP1](20-the-approaches/01_ap1-scale-and-foundation-models.md) | One simple job + huge scale → skills appear on their own. |
| the Bitter Lesson (Sutton) | `c-ap1-scale` → [AP1](20-the-approaches/01_ap1-scale-and-foundation-models.md) | Methods that use more compute beat hand-built human knowledge. |
| foundation model | `c-ap1-scale` → [AP1](20-the-approaches/01_ap1-scale-and-foundation-models.md) | One big pretrained machine everything else is built on. |
| scaling-suffices debate | `c-ap1-scale` → [AP1](20-the-approaches/01_ap1-scale-and-foundation-models.md) | Is scale alone enough for AGI, or is a second idea needed? |
| the "think longer" bet / test-time compute | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) | Spend computer power *at answer-time*, not just training-time. |
| chain-of-thought (show the steps) | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) | Write in-between steps → more compute per hard problem → better answers. |
| fast vs slow thinking (in AI) | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) | Snap one-pass answer vs step-by-step working-out. |
| reasoning-via-RL / verifiable rewards | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) | Reward only correct checkable answers → good reasoning emerges (DeepSeek-R1). *(Deep RL home = [AP4](20-the-approaches/04_ap4-rl-from-interaction.md).)* |
| reinforcement learning (reward learning) — minimal taste | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) intro | Try → get a score → shift toward what scored well. *(One-line version only; full frame → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md).)* |
| self-play / AlphaZero (durable ancestor) | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) | Learn a skill from reward + search + self-tries, no human examples. *(Self-play **at scale** → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md).)* |
| reasoning-generalization debate | `c-ap2-reasoning` → [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) | Does learned reasoning transfer past checkable problems, or just sharpen the base model? (open) |
| reinforcement learning (the full frame) | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Agent in a world: state → action → reward → repeat; learn by trial and error, no human answers. *(The deep home; AP2 gave the taste.)* |
| reward · return · policy · value | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Reward = score to maximize; return = its long-run total; policy = the strategy; value = expected future return. |
| explore vs exploit · credit assignment | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Use-what-works vs try-new; and which past action earned a late, sparse reward. |
| the reward hypothesis | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Any goal = "maximize one scalar score, summed over time" (Sutton & Barto). |
| "reward is enough" (the bet) | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Intelligence + all its abilities emerge from maximizing reward in a rich world (Silver et al. 2021). |
| self-play at scale (DQN / Dota) | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Superhuman skill grown from zero on its own experience — Atari pixels, OpenAI Five. |
| reward specification / reward hacking | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | A maximizer chases the number, not your intent; Goodhart made live and dangerous. |
| sample-inefficiency (RL) | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | RL needs a fortune of tries — fine in a simulator, brutal in the slow real world. |
| era of experience / grounded rewards | `c-ap4-rl` → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md) | Learn from your own life, not our text; reward from world signals — answers the data wall (Silver & Sutton 2025). |
| world model (predict consequences, then plan) | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | An inner copy of how the world changes: state + action → next state; plan by imagining outcomes and picking the best (LeCun). |
| JEPA / predict the abstract summary, not pixels | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | Joint Embedding Predictive Architecture — throw away unpredictable noise (the "windy leaves"), predict only what matters. |
| generative vs non-generative prediction | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | Draw every pixel of the future (failed 10 yrs) vs predict an abstract representation of it (JEPA's bet). |
| objective-driven AI / planning by optimization | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | Think/plan before you speak; model predictive control — imagine actions, predict, score, pick best (rockets since the '60s). |
| the sensory-bandwidth argument | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | A 4-year-old's eyes take in ~50× more than all internet text; most world-knowledge is watched, not read (LeCun). |
| autoregressive drift (exponential error) | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | Word-by-word output has a rising chance of wandering off the correct path — the longer it talks, the faster (LeCun). |
| hierarchical planning (open problem) | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | Plans built in layers (goal → sub-goals → muscle) — "nobody knows how to do this in AI" (LeCun). |
| the world-models race (SOTA snapshot) | `c-ap5-world-models` → [AP5](20-the-approaches/05_ap5-world-models-jepa.md) | 2025–26 surge: V-JEPA 2, DreamerV3 (Nature), AMI Labs ($1.03B), Genie 3, World Labs/Marble — dated, ages fast. |
| skill vs intelligence | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Stored, fetchable competence (skill) is *not* the power to handle the truly new (intelligence) — Chollet. *(AP1 only touched this line.)* |
| measure of intelligence / skill-acquisition efficiency | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Intelligence = how fast/cheaply you master a *novel* task from little data, not how much skill you already have (Chollet 2019). |
| ARC / ARC-AGI (the test) | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Little puzzles built to resist memorising — each novel, needs only a child's *core knowledge*. *(o3/test-time-compute number lives at [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md).)* |
| core knowledge (priors) | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | The few basics every 4-year-old has: objectness, counting, elementary physics — all ARC assumes. |
| program fetching vs synthesis | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Pull a stored recipe (fetch) vs build a new one on the fly for a problem you have no recipe for (synthesis). |
| program synthesis / discrete program search | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Search a small toolbox (DSL) for a program that fits the examples; data-thrifty (learns from 1–2) but compute-heavy (combinatorial explosion). |
| deep learning + program search (the merger) | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Curve-fitting intuition (System 1) *guides* the program search (System 2) — cut the explosion; Chollet's proposed road. |
| active inference / test-time training | `c-ap8-program-synthesis` → [AP8](20-the-approaches/08_ap8-program-synthesis-arc.md) | Let the model learn a little from the specific task before answering — a form of program synthesis; what unlocks ARC scores. |
| skill-acquisition-efficiency *formula* (the measure) | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | The *slogan* (skill-acquisition efficiency) turned into a fraction: generalization difficulty ÷ (priors + experience), averaged over a scope of tasks (Chollet 2019). *(One-line version + `measure of intelligence` home = [AP8 card](20-the-approaches/08_ap8-program-synthesis-arc.md); the formal machine is here.)* |
| generalization difficulty | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | The *top* of the fraction: how big a jump from training to test the free stuff (priors+experience) could **not** cover — the part where thinking had to happen. |
| priors + experience (the "buy skill" axis) | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | The *bottom* of the fraction: skill is bought two ways — built-in knowledge (priors) or practice data (experience); **summed** (so nature-vs-nurture doesn't matter to the measure). |
| the generalization spectrum | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | The ladder of jump-height: absence → local (robustness) → broad (flexibility) → extreme (human — "unknown unknowns"). |
| system-centric vs developer-aware generalization | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | New to the *system* (a chosen test set — cheap) vs new to the *system **and** its builder* (why ARC hides the test from everyone). |
| core knowledge — the four systems (enumerated) | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | The infant priors from developmental psychology, the fixed "floor": objectness/physics · agentness/goals · number/arithmetic · geometry/topology. *(One-line version = [AP8 card](20-the-approaches/08_ap8-program-synthesis-arc.md).)* |
| algorithmic information theory / program-length ruler | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | Measure "amount of understanding" as the length of the shortest program (Kolmogorov complexity) — short = a rule, long = a lookup table; uncomputable, so the formula is a *compass, not a meter*. |
| no universal intelligence / anthropocentric scope | `c-measure-of-intelligence` → [AP8 deep dive ⑤](50-deep-dives/01_ap8-deep-dive-the-formal-theory-of-intelligence.md) | No-free-lunch kills "good at all environments"; measure over a *bounded* scope, and pick the **human** one (contra Legg–Hutter's universal intelligence). |
| depth vs width of the search explosion | `c-arc-solver-mechanisms` → [AP8 deep dive #2 ⑤](50-deep-dives/02_ap8-deep-dive-solver-mechanisms.md) | The card's one "explosion" wall is really two: the program tree is too **deep** (long programs) and too **wide** (many wrong steps); every solver trick cuts one or both. |
| DreamCoder / wake–sleep library learning | `c-arc-solver-mechanisms` → [AP8 deep dive #2 ⑤](50-deep-dives/02_ap8-deep-dive-solver-mechanisms.md) | Ellis et al. 2021 — grow a **library** of reusable tools (chunk reused fragments → shorter programs, less depth) + train a **neural search policy** on self-invented "dream" programs (less width); the two **bootstrap** each other. A partial answer to "where do priors come from?" (the library is learned priors). |
| induction vs transduction (two solver families) | `c-arc-solver-mechanisms` → [AP8 deep dive #2 ⑤](50-deep-dives/02_ap8-deep-dive-solver-mechanisms.md) | **Induction** = write a program and run it (checkable, exact — Greenblatt's ~1000 GPT-4o programs/task); **transduction** = paint the answer grid directly (fuzzy, perceptual). Same data + architecture → they solve *different* puzzles; ensembling ≈ human-level (Li, Ellis et al. 2024). |
| test-time training (the 2024 winning recipe) | `c-arc-solver-mechanisms` → [AP8 deep dive #2 ⑤](50-deep-dives/02_ap8-deep-dive-solver-mechanisms.md) | **Augment** the 2–3 examples (rotate/reflect/recolour) → **fine-tune** the model per puzzle → **vote** for the answer most stable across transformed views (the ARChitects, "a matter of perspective," 53.5% ARC-AGI-1, 2024). The full recipe behind the card's one-line "active inference." |
| the refinement loop (propose → check → revise) | `c-arc-solver-mechanisms` → [AP8 deep dive #2 ⑤](50-deep-dives/02_ap8-deep-dive-solver-mechanisms.md) | The defining shape of 2025 — evolutionary per-task search steered by a **checker**; Berman evolves *natural-language* descriptions (more expressive than code, but can't be run → the checker mattered more than the proposer), 29.4% ARC-AGI-2-pub. |
| compression at test time (CompressARC) | `c-arc-solver-mechanisms` → [AP8 deep dive #2 ⑤](50-deep-dives/02_ap8-deep-dive-solver-mechanisms.md) | Liao & Gu 2025 — a tiny net, no pretraining/dataset/search, trains per puzzle to *compress* it (minimum description length) → 20% eval. Deep dive #1's program-length ruler turned into a working solver. |
| growing the priors in an open world | `c-arc-research-frontier` → [AP8 deep dive #3 ⑤](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) | The card's Stuck #1, at the frontier: **soft/neural** library learning (a net that "produces similar code," not an explicit list) · **dream-and-chase** wake–sleep to stretch to harder-than-trained problems (distribution shift) · abstractions defined **per problem** (resource-rationality), not one true set. Deep dive #2's DreamCoder grows the *middle* of the tower; this is growing the *whole vocabulary* in an open world — still open. |
| latent program search / nets that propose programs (LPN) | `c-arc-research-frontier` → [AP8 deep dive #3 ⑤](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) | A **third** shape of neural help past #2's neural-*guided* search and Greenblatt's LLM-writes-programs: encode the examples into a point on a **learned smooth map of programs**, nudge it by gradient at test time, decode it onto the new input — **no hand-built DSL**, no internet priors (Bonnet & Macfarlane, *"Searching Latent Program Spaces,"* ~10% eval, search doubles OOD). |
| the compositionality wall (superposition vs composition) | `c-arc-research-frontier` → [AP8 deep dive #3 ⑤](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) | A fixed continuous program-space can **blend** known programs (superposition) but not **chain** arbitrary new steps (composition) → stalls on deep "type-2" puzzles. The latest form of [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md)'s 35-year **systematicity** objection; the wall neural program-*proposal* keeps hitting. |
| efficiency as the problem statement / cost-capped competition | `c-arc-research-frontier` → [AP8 deep dive #3 ⑤](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) | Deep dive #1's *efficiency* turned into a competition rule: a capability bought with a huge, expensive search has not shown intelligence, only brute force. The ARC Prize's top prize is **cost-capped** (open-source, fixed compute budget on Kaggle) — so o3's ~$3.5k/task run is disqualified by design; progress = matching an unbounded search *cheaply*. |
| the interactive / agentic turn (ARC-AGI-3, "active ARC") | `c-arc-research-frontier` → [AP8 deep dive #3 ⑤](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) | ARC-AGI-3 (launched 25 Mar 2026) is the first **interactive** ARC — turn-based game-worlds with *"no instructions, no rules, and no stated goals"*; the agent must **explore → model → set goals → plan** to find the puzzle before solving it (humans 100% / frontier ~0.5%). Reaches into [AP4](20-the-approaches/04_ap4-rl-from-interaction.md)/[AP5](20-the-approaches/05_ap5-world-models-jepa.md)/[AP10](20-the-approaches/10_ap10-embodiment.md); Ellis' "active ARC" is the program-synthesis side of the same turn. |
| program search that discovers new knowledge (FunSearch) — the bridge to open-endedness | `c-arc-research-frontier` → [AP8 deep dive #3 ⑤](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) | AP8's engine pointed *outward*: an LLM **proposes** programs, an exact **evaluator** runs+scores them, a database keeps the best (FunSearch, DeepMind, *Nature* 2023 — new cap-set + bin-packing results). Searches the **program that generates** answers, so a short winner is a *general* discovery (deep dive #1's ruler). Run the loop forever → it **becomes** [AP9 · open-endedness](20-the-approaches/09_ap9-open-endedness.md) (which owns its successor AlphaEvolve). |
| temporal-difference learning ("a guess from a guess") / the TD error | `c-rl-the-engine` → [AP4 deep dive #1 ⑤](50-deep-dives/04_ap4-deep-dive-the-engine.md) | The heart of RL, the engine the [AP4 card](20-the-approaches/04_ap4-rl-from-interaction.md) named but never opened: learn a situation's **value** by nudging each guess toward *reward + next-situation guess* (bootstrapping), so real reward "injects reality" one step at a time. The gap between the two guesses is the **TD error = surprise**, and it seeps credit backward through time (solving the card's credit-assignment problem without ever asking "which move earned it?"). |
| dopamine = reward-prediction-error (the TD error in the brain) | `c-rl-the-engine` → [AP4 deep dive #1 ⑤](50-deep-dives/04_ap4-deep-dive-the-engine.md) | The most beautiful AI↔brain bridge: dopamine neurons fire not for reward but for **surprise about** reward (burst at unexpected reward, silent at predicted, dip at withheld) — tick-for-tick the **TD error** (Schultz, Dayan & Montague 1997; Montague, Dayan & Sejnowski 1996; deepened by Dabney et al., *Nature* 2020, distributional coding). A real point *for* the card's "reward is enough" bet — but a match, not a proof. |
| value-based vs policy-based vs actor-critic | `c-rl-the-engine` → [AP4 deep dive #1 ⑤](50-deep-dives/04_ap4-deep-dive-the-engine.md) | Two ways to turn learning into doing: **value-based** (learn each action's worth **Q**, act greedily — Q-learning/DQN; off-policy, sample-thrifty, but stuck on huge/continuous action sets) vs **policy-based** (adjust the way you act directly — REINFORCE; fluent in continuous actions, but noisy and on-policy). **Actor-critic** runs both — an actor acts, a TD-critic judges by surprise; **PPO** is the workhorse (behind OpenAI Five *and* RLHF). |
| model-free vs model-based RL / MuZero | `c-rl-the-engine` → [AP4 deep dive #1 ⑤](50-deep-dives/04_ap4-deep-dive-the-engine.md) | **Model-free** = learn a reflex (value/policy) from real tries only — simple, general, and *structurally* sample-hungry (this is *why* the card's Stuck #2 exists). **Model-based** = learn a world-model and plan/imagine inside it (**MuZero** learns its own model and plans without being given the rules — Schrittwieser et al., *Nature* 2020). The escape from sample-inefficiency, and the deep home of the model-based branch is [AP5 · world models](20-the-approaches/05_ap5-world-models-jepa.md). |
| the deadly triad | `c-rl-the-engine` → [AP4 deep dive #1 ⑤](50-deep-dives/04_ap4-deep-dive-the-engine.md) | The three most *desirable* ingredients of deep RL — neural nets + bootstrapping + off-policy reuse — can **diverge** when combined. Why the clean tabular theory doesn't transfer to real worlds, and why much deep-RL craft (target networks, small trust-region steps, GRPO dropping the critic) is really triad-stabilisation. |
| the RL engine behind today's models (RLHF/PPO → verifiable-reward/GRPO) | `c-rl-the-engine` → [AP4 deep dive #1 ⑤](50-deep-dives/04_ap4-deep-dive-the-engine.md) | The card's "RL came back" made mechanical: the **actor-critic** engine, with the "agent" a language model and the reward a **learned reward model** (from human-preference pairs), *is* **RLHF/PPO** — which *relocates* (not removes) reward hacking. Swap the learned reward for a **checkable** one and use **GRPO** (drops the critic, scores a group vs its average) → 2025's reasoning models. Frontier homes = [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) + [alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md). |
| self-supervised learning (hide-a-part, predict-it) | `c-jepa-self-supervision` → [AP5 deep dive #1 ⑤](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md) | How you learn from raw video with **no human labels** (the only affordable fuel for the [AP5](20-the-approaches/05_ap5-world-models-jepa.md) bet): hide part of the data and predict it from the rest — the "right answer" is the hidden part, free. The **same idea** as the [next-word](10-how-ai-works-today/01_guessing-the-next-word.md) machine, but a JEPA predicts in the **summary space**, not the raw input. The made-up "predict the hidden patch" job = a **pretext task**. |
| representational collapse (the "constant vectors" cheat) | `c-jepa-self-supervision` → [AP5 deep dive #1 ⑤](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md) | The trapdoor under the [AP5 card](20-the-approaches/05_ap5-world-models-jepa.md)'s "predict-the-summary" slogan: a machine trained to make its own summaries predictable **cheats** by making *every* summary identical → prediction is perfect and meaning is zero ("*A trivial solution is obtained when the encoder outputs constant vectors*" — VICReg). Predicting raw pixels is *safe* from this; predicting a summary you also control is not. **Beating collapse IS the JEPA problem.** |
| the three anti-collapse guards (contrastive · VICReg · distillation) | `c-jepa-self-supervision` → [AP5 deep dive #1 ⑤](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md) | Almost every self-supervised system is one of three ways to stop [collapse]: **contrastive** (push **negatives** apart — SimCLR), **regularized** (a rule that forces the summaries to stay spread out — VICReg's variance term, no negatives), or **distillation** (chase a slow **EMA** copy of yourself behind a **stop-gradient** — BYOL/DINO; the trick **real JEPAs use**, plus masking). The guards *work in practice*; the clean theory of *why* is still argued (LeJEPA, 2025, claims to be the first to drop the heuristics). |
| the JEPA lineage: I-JEPA → V-JEPA → V-JEPA 2 | `c-jepa-self-supervision` → [AP5 deep dive #1 ⑤](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md) | The [AP5 card](20-the-approaches/05_ap5-world-models-jepa.md) named V-JEPA 2 in one line; here is the ladder. **I-JEPA** (Assran et al., 2023 — mask blocks of *one image*, predict their summaries; "*a non-generative approach … from a single context block, predict the representations of various target blocks*") → **V-JEPA** (2024 — the same across *time* in video) → **V-JEPA 2** (Meta, 2025 — **>1M hours** of internet video; 77.3 SSv2, 39.7 R@5 Epic-Kitchens). Proof the "predict the summary, not pixels" trick learns useful features at scale. |
| V-JEPA 2-AC — the latent action-conditioned world model | `c-jepa-self-supervision` → [AP5 deep dive #1 ⑤](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md) | The card's bet standing up: post-train V-JEPA 2 on **<62 hours** of unlabelled robot video to make it **action-conditioned** (predict *state + action → next state* in the **latent** summary space) → a world model a robot **plans** inside. Deployed **zero-shot** on Franka arms in two labs, it does real pick-and-place **with no reward, no task-specific training** (Meta, 2025). This is [AP4](20-the-approaches/04_ap4-rl-from-interaction.md)'s *model-based* branch realised in a learned latent space — the non-generative bet, working (narrowly). |
| energy-based models / objective-driven AI / H-JEPA | `c-jepa-self-supervision` → [AP5 deep dive #1 ⑤](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md) | The durable framing: don't output an answer, **score how well two things fit** (low **energy** = compatible) — and collapse is just "give everything low energy." The JEPA is one **module** in LeCun's *A Path Towards Autonomous Machine Intelligence* (2022): perception + world-model + cost + actor + memory. The proposed answer to the [AP5 card](20-the-approaches/05_ap5-world-models-jepa.md)'s **hierarchical-planning** crack (Stuck #3) is a **stack of JEPAs (H-JEPA)** — a *proposal*, still open. |
| the archive loop (keep → vary → keep) — the one shape | `c-open-ended-engine` → [AP9 deep dive #1 ⑤](50-deep-dives/06_ap9-deep-dive-the-open-ended-engine.md) | The machine under all of the [AP9 card](20-the-approaches/09_ap9-open-endedness.md)'s tools: every open-ended method is *keep an **archive** → **vary** something in it → **select**/keep the good variants → repeat*. Methods differ **only** in the variation step and the keep-rule — and the keep-rule is the whole game (a naive "keep only if it beats the champion" throws away the **stepping stones** the objective paradox needs). |
| novelty search, mechanically (behaviour space + k-NN sparseness) | `c-open-ended-engine` → [AP9 deep dive #1 ⑤](50-deep-dives/06_ap9-deep-dive-the-open-ended-engine.md) | How "reward being new" becomes a number (Lehman & Stanley, 2011): describe a thing by what it **does** (a **behaviour characterization**), not how it's **built** (its **genome**); then novelty = **average distance to the k nearest neighbours** in behaviour space (**sparseness**, `ρ(x)=(1/k)·Σ dist(x,μᵢ)`). Keep the far-out ones. It never looks at the goal → can't be **deceived**. |
| quality-diversity, mechanically (MAP-Elites illumination grid) | `c-open-ended-engine` → [AP9 deep dive #1 ⑤](50-deep-dives/06_ap9-deep-dive-the-open-ended-engine.md) | The card's "keep the best of every kind" as a machine (Mouret & Clune, 2015): **tessellate** behaviour space into a grid of cells, keep the single best **elite** per cell; loop = pick a random elite → **perturb** → read its **fitness** + **behaviour descriptor** → place it if the cell is empty or it beats the resident. The **filled map is the output** (a shelf of stepping stones) — *"illumination, not optimisation"*; the axes are *"dimensions of variation that a user gets to choose."* |
| POET, mechanically (minimal criterion + transfer) | `c-open-ended-engine` → [AP9 deep dive #1 ⑤](50-deep-dives/06_ap9-deep-dive-the-open-ended-engine.md) | How you generate the *problems* (Wang/Lehman/Clune/Stanley, 2019): the **minimal criterion** keeps a new environment only if it's neither too easy nor too hard (*"a lower and upper bound…filters out environments that are either too simple or too complicated"*) = *novel-and-learnable* as a filter → an **automatic curriculum**; **transfer** periodically moves any solver to a problem it does better on (*"replaced if a transfer is better"*) = the **stepping-stone** idea as a subroutine, the answer to [AP4](20-the-approaches/04_ap4-rl-from-interaction.md)'s "who writes the reward?" |
| the foundation-model turn (a model in two slots) / verifiable-vs-judged | `c-open-ended-engine` → [AP9 deep dive #1 ⑤](50-deep-dives/06_ap9-deep-dive-the-open-ended-engine.md) | The 2025–26 systems (**AlphaEvolve**, the **Darwin Gödel Machine**, ICLR 2026) are the *same archive loop* with a foundation model in two slots: **variation** (it *writes* smart variants, not random tweaks — the [FunSearch loop](50-deep-dives/03_ap8-deep-dive-the-research-frontier.md) scaled up) and, where no exact score exists, **selection** (it judges "interesting" — OMNI). The split that matters: **exact checker** (real, checkable discovery) vs **model-as-judge** (bounded by frozen human taste). |
| the open-ended engine's four cracks (chosen-measure · thin-loop · needs-a-checker · stalling) | `c-open-ended-engine` → [AP9 deep dive #1 ⑤](50-deep-dives/06_ap9-deep-dive-the-open-ended-engine.md) | The *engine's own* limits (distinct from the [card](20-the-approaches/09_ap9-open-endedness.md)'s bet-cracks): the "interesting" measure is **chosen by a human**, not discovered (sharpens the card's Stuck #1) · the loop is **thin**, the **operator** (a foundation model) does the work (mechanical Stuck #2) · real discovery needs an **exact checker**, and most of the open world has none (new) · **stalling** is the loop's default and every anti-stall device is a hand-added patch — like [AP5](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md)'s anti-collapse guards. Big question: the engine solved the *plumbing* but supplies neither its own **measure** nor its own **fuel**. |
| the scaling-law equation (floor + two penalties) | `c-scaling-law-anatomy` → [AP1 deep dive #1 ⑤](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md) | The equation under the [scaling-laws rung](10-how-ai-works-today/02_scaling-laws-and-emergence.md)'s straight line (Hoffmann et al., 2022): `L(N,D) = E + A/N^α + B/D^β` — an unbeatable **floor** `E` (the *entropy of natural text*), plus a **size-penalty** `A/N^α` you pay down with parameters, plus a **data-penalty** `B/D^β` you pay down with tokens. The exponents α≈0.34, β≈0.28 are **small**, so each equal slice of progress costs a *multiplying* amount of compute. |
| the compute identity (`C ≈ 6ND`) | `c-scaling-law-anatomy` → [AP1 deep dive #1 ⑤](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md) | Where compute meets size and data (Kaplan et al., 2020): a training run costs about **6 × parameters × tokens** (≈2 FLOPs/param/token forward + ≈4 backward). A fixed budget fixes `N×D`, so size and data **trade off** — and minimising the loss along that budget line **is** the compute-optimal/Chinchilla problem (a solved constrained-optimisation, not a rule of thumb — the *mechanism* under the [rung](10-how-ai-works-today/02_scaling-laws-and-emergence.md)'s answer). |
| reading the future off the line (fit-and-extrapolate) | `c-scaling-law-anatomy` → [AP1 deep dive #1 ⑤](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md) | How labs predict a model *before* building it: train a **ladder** of small cheap models → fit the five pieces (E,A,B,α,β) → **extend the line** to the giant target. Real — the GPT-4 team predicted its final loss from models using *"no more than 1/1,000th the compute,"* locked in *"without use of any partial results"* (GPT-4 report, 2023). But what's predicted is **loss**, not capability. |
| the two-year measurement bug (Kaplan→Chinchilla) | `c-scaling-law-anatomy` → [AP1 deep dive #1 ⑤](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md) | Why an empirical fit can be confidently wrong: the 2020 law pointed the field at **oversized, under-fed** models for two years. Causes (2024 reconciliation): Kaplan counted **non-embedding** parameters/compute (not total), and used a **learning-rate schedule not matched** to each short run — both tilting the optimum toward "size." A ~3× wrong shape, and the wrong line looked as clean as the right one. The lesson: a scaling law inherits every quiet bias in the runs you fit it on. |
| the 2026 frontier (data-constrained · precision · test-time axis) | `c-scaling-law-anatomy` → [AP1 deep dive #1 ⑤](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md) | The law re-fit for the real world: **data-constrained** — repeating text gives ~4 near-free epochs then decays to zero (Muennighoff et al., 2023, the mechanical [data wall](10-how-ai-works-today/02_scaling-laws-and-emergence.md)); **precision** — fewer bits ≈ fewer *effective parameters*, and after compression more data can *hurt* (Kumar et al., 2024); and a **new axis** — **test-time compute** (o1, 2024: accuracy scales with *thinking at answer-time*, paid every answer). The reasoning *mechanism* = [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md); only the scaling-*shape* is here. |
| the scaling law's four engine cracks (no-theory · loss≠capability · cruel-exponents · mis-measured-once) | `c-scaling-law-anatomy` → [AP1 deep dive #1 ⑤](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md) | The *instrument's own* limits (distinct from the [card](20-the-approaches/01_ap1-scale-and-foundation-models.md)'s bet-cracks): it's **measured, not derived** — no accepted theory of *why* the power law holds (candidates: data-manifold-dimension, the quantization/quanta model — none settled, 2026) · it predicts **loss, not capability** · its **small exponents** make progress *exponentially costlier* (the arithmetic under the [physical limits](30-across-the-approaches/01_the-bounds-data-compute-energy.md)) · it was **mis-measured once**, for years. Big question: the most reliable fact in AI is the least safe thing to **extrapolate** to AGI. |
| the agent control loop (stateless model + harness) | `c-agent-loop-context` → [AP3 deep dive #1 ⑤](50-deep-dives/08_ap3-deep-dive-inside-the-agent-loop.md) | The machine under the [AP3 card](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md)'s "model in a loop": a language model is a **stateless** function (no memory between calls), so a plain program — the **harness** — runs the three-step loop (*"LLM determines the next step…outputting structured json…Deterministic code executes the tool call…The result is appended to the context window"* — 12-Factor Agents) and holds all the state. The agent's continuity lives in the harness, **not** the model. |
| tool-calls as structured output (+ MCP) | `c-agent-loop-context` → [AP3 deep dive #1 ⑤](50-deep-dives/08_ap3-deep-dive-inside-the-agent-loop.md) | A model never *acts* — it emits a **wish in structured text** (a tool's name + arguments as JSON) that *deterministic code* executes ("tools are just structured outputs"). A **schema** tells the model what it may request (*"schema-defined interfaces that LLMs can invoke"* — MCP docs); **MCP** (Model Context Protocol) is the 2026 standard plug — *"a standardized way to connect AI applications to external systems"* (USB-C-for-AI), ~97M monthly downloads / 10k+ servers cross-lab (2026-07). |
| the context window as the agent's whole world (+ context engineering) | `c-agent-loop-context` → [AP3 deep dive #1 ⑤](50-deep-dives/08_ap3-deep-dive-inside-the-agent-loop.md) | On each turn the model knows **only** the finite page fed to it — instructions + tools + transcript + latest result; nothing else exists to it. So the craft is **context engineering**: *"finding the smallest possible set of high-signal tokens"* (Anthropic, 2025). Memory, planning, tools, and skills are all just **writing to / reading from that one page.** |
| context rot / lost-in-the-middle / the n² attention cost | `c-agent-loop-context` → [AP3 deep dive #1 ⑤](50-deep-dives/08_ap3-deep-dive-inside-the-agent-loop.md) | Why a *fuller* page is a *worse* page: models attend best to the ends and miss the middle (*"significantly degrades…in the middle of long contexts"* — Liu et al., 2023) and recall falls as the window fills (**context rot**), because attention is **n²** — every token relates to every other, so doubling the text ~quadruples the work. *"Context…must be treated as a finite resource with diminishing marginal returns"* (Anthropic). The resource root of the card's **march of nines**. |
| memory & planning as context management (compaction + retrieval; sub-agents) | `c-agent-loop-context` → [AP3 deep dive #1 ⑤](50-deep-dives/08_ap3-deep-dive-inside-the-agent-loop.md) | The card's memory/reflection/planning, mechanically: **compaction** (summarise the transcript to fit) + **retrieval** (store outside, pull back the relevant slice — RAG/embeddings); the plan is just a to-do list *on the page* that can scroll off. Every recalled token is **rent on the window.** The 2026 multi-agent that works = **sub-agents as context isolation** (each a clean page condensing a summary back — Anthropic; but 4×–15× the tokens). |
| the agent loop's four engine cracks (finite-window · un-absorbable-harness · tool-call-nines · lossy-memory) | `c-agent-loop-context` → [AP3 deep dive #1 ⑤](50-deep-dives/08_ap3-deep-dive-inside-the-agent-loop.md) | The *loop's own* limits (distinct from the [card](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md)'s bet-cracks): **one finite, degrading window** everything competes for (the resource under the march of nines) · the **harness** (loop + tool-execution + window management) can **never** be absorbed into a stateless forward pass, so *some* scaffolding is permanent (locates the card's Stuck #2) · **tool-calling has its own march of nines** (a call can be malformed / mis-aimed / hallucinated) · **memory is lossy retrieval, not recall.** Big question: the 2026 wall is a **context-handling gap, not a reasoning gap.** |
| sequential vs parallel test-time compute | `c-test-time-compute-machine` → [AP2 deep dive #1 ⑤](50-deep-dives/09_ap2-deep-dive-how-a-machine-thinks-longer.md) | The machine under the [AP2 card](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md)'s single "think longer" dial: answer-time compute is spent two different ways — **sequential** (one longer chain of thought — depth) or **parallel** (many independent chains, then combine — width; **self-consistency** = *"samples a diverse set of reasoning paths … selects the most consistent answer by marginalizing"*, i.e. a majority vote — Wang et al. 2022). Different machines, different walls; real systems mix both. |
| coverage vs selection (the repeated-sampling power law) | `c-test-time-compute-machine` → [AP2 deep dive #1 ⑤](50-deep-dives/09_ap2-deep-dive-how-a-machine-thinks-longer.md) | The split the card's one rising curve hides: **generation** is nearly solved — sample enough and a right answer appears in the pile (**coverage** *"scales with the number of samples over four orders of magnitude"* as a power law; DeepSeek-Coder 15.9%@1 → 56%@250 samples, beating 43% single-shot — Brown et al. 2024). But **selection** is the ceiling: *"in domains without automatic verifiers … majority voting and reward models … plateau."* Generation scales; selection plateaus. |
| the verification asymmetry (checking easier than generating) | `c-test-time-compute-machine` → [AP2 deep dive #1 ⑤](50-deep-dives/09_ap2-deep-dive-how-a-machine-thinks-longer.md) | Why spending compute at answer-time works at all: for many problems **verifying** a candidate is far easier than **producing** one, so you generate many cheap candidates and spend compute checking them. This is [AlphaZero](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md)'s **propose-and-evaluate** loop (MCTS) pointed at reasoning steps. Where a perfect checker exists (compiler, maths) → soar; where not → you must build one (a reward model). |
| process vs outcome reward models (PRM > ORM) | `c-test-time-compute-machine` → [AP2 deep dive #1 ⑤](50-deep-dives/09_ap2-deep-dive-how-a-machine-thinks-longer.md) | The learned verifier's key design choice (Lightman et al. 2023): an **outcome** reward model grades only the final answer; a **process** reward model grades *"each intermediate reasoning step."* Finding: *"process supervision significantly outperforms both forms of outcome supervision"* — grading steps localises errors and steers a search at every node. But the PRM is *itself a fallible model*, often a sibling of the generator (the card's unfaithful-CoT crack, from the verifier's side). |
| search / Tree of Thoughts | `c-test-time-compute-machine` → [AP2 deep dive #1 ⑤](50-deep-dives/09_ap2-deep-dive-how-a-machine-thinks-longer.md) | The richest spend: a **tree** of partial thoughts you grow and prune — *"deliberate decision making by considering multiple different reasoning paths and self-evaluating choices … looking ahead or backtracking"* (Yao et al. 2023; Game of 24: CoT 4% → ToT far more). Unifies the family: sequential = a **path**, parallel = a **fan**, search = a **tree** — all generate-and-select, all lean on the verifier. AlphaZero's template transfers, but its *perfect simulator + clean reward* do not. |
| the test-time-compute cracks (compute-optimal · overthinking · verifier-bottleneck) | `c-test-time-compute-machine` → [AP2 deep dive #1 ⑤](50-deep-dives/09_ap2-deep-dive-how-a-machine-thinks-longer.md) | The *machine's own* limits (distinct from the [card](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md)'s bet-cracks): **the bottleneck is verification, not generation** (sharpens "needs a checker" into the central mechanism) · the **verifier is a fallible, gameable model** (reward hacking → [AP4](20-the-approaches/04_ap4-rl-from-interaction.md)) · **both curves bend** — sequential *overthinks* past a knee, parallel is capped by verifier false-positives; a compute-optimal small model can beat a *"14x larger model"* (Snell et al. 2024) · the **AlphaZero template transfers but its perfect checker does not.** Big question: the frontier of "think longer" is really a **verification** problem. |
| predictive coding (predictions down / errors up) | `c-predictive-coding-learning-rule` → [AP6 deep dive #1 ⑤](50-deep-dives/10_ap6-deep-dive-predictive-coding-and-the-learning-rule.md) | The concrete mechanism under the [AP6 card](20-the-approaches/06_ap6-brain-based.md)'s "the predictive brain" (Rao–Ballard; Huang & Rao 2011): the cortex is a **hierarchy** where **feedback carries predictions down** and **feedforward carries only the residual error up** — perception is a generative model's guess, corrected by surprise (*"By transmitting only the unpredicted portions … reduce redundancy … limited dynamic range"*). Same predict-and-correct shape as self-supervised AI / [JEPA](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md). Distinct from [AP4](50-deep-dives/04_ap4-deep-dive-the-engine.md)'s *reward* (dopamine) prediction error — this is *sensory*. |
| the free-energy principle (minimise surprise) | `c-predictive-coding-learning-rule` → [AP6 deep dive #1 ⑤](50-deep-dives/10_ap6-deep-dive-predictive-coding-and-the-learning-rule.md) | Friston's grand form: any self-organising system minimises **free energy**, a *"bound on the surprise inherent in any exchange with the environment"* — lowered two ways: **perception** (change your beliefs to fit the world = predictive coding) or **action** (change the world to fit your beliefs = *active inference*). Unifies perception/action/learning; a cousin of [AP5](50-deep-dives/05_ap5-deep-dive-learning-without-a-teacher.md)'s energy-based view. Criticised as possibly too general to falsify. |
| why the cortex can't run backpropagation | `c-predictive-coding-learning-rule` → [AP6 deep dive #1 ⑤](50-deep-dives/10_ap6-deep-dive-predictive-coding-and-the-learning-rule.md) | The concrete brain-vs-AI gap the [card](20-the-approaches/06_ap6-brain-based.md) skips: modern AI learns by **backpropagation** (the [backward pass](50-deep-dives/07_ap1-deep-dive-anatomy-of-a-scaling-law.md)); the brain almost certainly can't. Two reasons: the **weight-transport problem** (backprop needs the *transpose* of forward weights → a two-way synapse; real synapses are one-way) and **non-locality** (backprop's updates need far-away error signals; biology learns **locally** — Hebbian / **STDP**). *"Backprop is often criticised for lacking biological plausibility"* (Millidge et al. 2020). |
| predictive coding approximates backprop | `c-predictive-coding-learning-rule` → [AP6 deep dive #1 ⑤](50-deep-dives/10_ap6-deep-dive-predictive-coding-and-the-learning-rule.md) | The 2020s bridge (Millidge et al. 2020): predictive coding *"converges asymptotically … to exact backprop gradients … using only local learning rules,"* and *"perform[s] equivalently to backprop … while utilising only local and (mostly) Hebbian plasticity."* So the brain can **match** the algorithm AI already uses — with only local, biologically-real machinery. Double-edged: it shows the brain's learning is a *plausible implementation* of gradient descent, not a *superior algorithm* (feedback alignment is a weaker cousin). |
| spiking neural networks / neuromorphic computing | `c-predictive-coding-learning-rule` → [AP6 deep dive #1 ⑤](50-deep-dives/10_ap6-deep-dive-predictive-coding-and-the-learning-rule.md) | What the [card](20-the-approaches/06_ap6-brain-based.md)'s Loihi/NorthPole name-drop actually is: real neurons fire discrete **spikes** (*"a short voltage pulse of 1-2 ms … about 100 mV"* — Gerstner), sparse and event-driven; **neuromorphic** chips copy this + put memory next to compute (killing the **von Neumann bottleneck**) → **25–100× energy efficiency** — but only for sparse workloads, **incompatible with backprop training**, too small for frontier models, so **stranded** ("the deployment gap is … a software and ecosystem problem", 2026). |
| the AP6-mechanism cracks (capability vs implementation) | `c-predictive-coding-learning-rule` → [AP6 deep dive #1 ⑤](50-deep-dives/10_ap6-deep-dive-predictive-coding-and-the-learning-rule.md) | The *mechanisms'* own limits (distinct from the [card](20-the-approaches/06_ap6-brain-based.md)'s bet-cracks): predictive coding is a **leading theory the brain data can't yet confirm** (underdetermined) · the free-energy principle **explains everything, predicts little** (near-unfalsifiable) · the brain's learning, made precise, **matches backprop, doesn't beat it** (sharpens "the brain-ignorers are winning") · the efficient neuromorphic substrate is **stranded from the capable software** (sharpens "right but too slow"). Big question: is the brain's edge a new **capability** or a better **implementation**? — 2026 evidence leans **implementation.** |
| the Kautz six-type dial (loose → tight join) | `c-neurosymbolic-integration` → [AP7 deep dive #1 ⑤](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md) | The real map under the [AP7 card](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md)'s "six types" name-drop (Kautz, AAAI-2020; via Wang, Yang & Wu 2024): every hybrid lines up on one dial — **loose** (Types 1–3: neural and symbolic in *separate boxes*, a clean human-built pipe between — a language model; AlphaGo's search calling a neural evaluator; a perception-net feeding a symbolic reasoner) → **tight** (Types 5–6: logic pushed *into* the network's loss or architecture). Where a system sits decides whether "no gradient through logic" is **avoided** or must be **solved**. |
| the loose join (neuro proposes, discrete tool disposes) | `c-neurosymbolic-integration` → [AP7 deep dive #1 ⑤](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md) | The *mechanism* under the card's **AlphaGeometry** result (DeepMind 2024): an exact **deduction engine** (a deductive database + algebra) grinds until stuck, then a **neural net** proposes *one* new object — *"the language model adds one potentially useful construct, opening new paths of deduction … This loop continues until a solution is found."* The seam passes **discrete messages**, so **no gradient crosses it** — the integration problem is **sidestepped, not solved**; cost = a **hand-built engine and seam, per domain** (nothing about the join is learned). Same shape as any model calling a calculator / theorem-prover / code interpreter. |
| the tight join / differentiable (soft, fuzzy) logic | `c-neurosymbolic-integration` → [AP7 deep dive #1 ⑤](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md) | The real attempt to *solve* "no gradient through logic": make the logic **soft** so a gradient can flow through it. *"Relax boolean first-order logic as soft fuzzy logic … approximate instead of fixed and exact"* — truth becomes a **degree in [0, 1]**, operators (∧ ∨ ¬ ⇒, ∃ ∀) get **differentiable** versions (Wang, Yang & Wu 2024). Two wirings: **logic-as-loss** (LTN, semantic loss — a rule becomes a soft term added to the training loss) and **logic-as-probabilistic-program** (**DeepProbLog** — *"neural networks as predicates … to compute the gradient of the desired loss"*), fusing learner + reasoner end-to-end. |
| the three taxes of the tight join | `c-neurosymbolic-integration` → [AP7 deep dive #1 ⑤](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md) | Softening the logic to make it trainable breaks the three things logic was *for*: **(1) exactness** — a soft rule is *usually mostly* obeyed, not *always exactly* (the works-in-every-case guarantee is gone); **(2) lawfulness** — the operators that learn best *"no longer satisfy the usual logical laws"* (van Krieken et al. 2022; and common soft-∧ vanishing gradients); **(3) scale** — exact inference over a logic program is **#P-hard** and blows up combinatorially, so the elegant end-to-end demos stay small. Each tax = the same coin: the softening that let learning in is what strips the logic of its worth. |
| no fully-fused system exists (Type 6) / the 2026 verdict | `c-neurosymbolic-integration` → [AP7 deep dive #1 ⑤](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md) | The tight end of the dial — a symbolic reasoning engine embedded *inside* a neural engine, exact *and* trainable end-to-end (Kautz's **Type 6**, the machine that would actually fulfil the bet) — does not exist: *"there is no NeSy approach to-date that can truly meet the standard of Type 6"* (Wang, Yang & Wu 2024). And the loose join is challenged from the other side: the neurosymbolic pipeline (AlphaProof + AlphaGeometry 2) won Math-Olympiad **silver** in 2024, but the **2025 gold** went to a *plain, end-to-end* reasoning model with no bolted-on proof-search pipeline — the [Bitter Lesson](20-the-approaches/01_ap1-scale-and-foundation-models.md) absorbing the hand-built seam *(SOTA as of 2026-07)*. |
| the AP7-mechanism cracks (the fork: exactness vs learnability) | `c-neurosymbolic-integration` → [AP7 deep dive #1 ⑤](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md) | The *mechanisms'* own limits (distinct from the [card](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md)'s bet-cracks): the join that works **avoids rather than integrates** (loose = sidestep the gradient) · the tight join **dissolves the exactness that was the point** · the honest tight join **doesn't scale** (#P-hard) · the working join is **hand-built and scale absorbs it** (the 2024 IMO **silver** neurosymbolic pipeline was outdone by a *plain end-to-end* model at **gold** in 2025). Big question: is there any join that is **both exact and learnable end-to-end**, or are those in permanent tension? — the fully-fused **Type 6** *"no NeSy approach to-date can truly meet."* |
| the VLA representation fork (discrete tokens vs continuous) | `c-robot-brain-vla` → [AP10 deep dive #1 ⑤](50-deep-dives/12_ap10-deep-dive-inside-the-robot-brain.md) | How a robot brain turns a decision into a motor command — the *mechanism* under the [AP10 card](20-the-approaches/10_ap10-embodiment.md)'s "actions as tokens." Either **discrete tokens** (chop each action dimension into bins — RT-1: *"discretized into 256 bins"* — and predict them one at a time, autoregressively, like words — RT-2/OpenVLA; simple, but coarse, slow at *"3 Hz,"* and averages away the several right ways to do a task) **or continuous diffusion/flow-matching** (sculpt the whole action from noise — Diffusion Policy: *"a conditional denoising diffusion process"* — precise, fast, and *"gracefully handling multimodal action distributions"*). Dexterity pushed the field to the continuous side. |
| the diffusion / flow-matching action expert | `c-robot-brain-vla` → [AP10 deep dive #1 ⑤](50-deep-dives/12_ap10-deep-dive-inside-the-robot-brain.md) | The continuous-action head bolted onto a web-trained VLM. **π0** *"[produces] actions via flow matching, a variant of diffusion"* using *"a novel action expert that augments the standard VLM with flow-based outputs"* — start from noise, nudge toward a valid action, emit a whole smooth sequence at once. Buys back what discrete tokens lose: precision, *"high-frequency action chunks (up to 50 Hz),"* and honest **multimodality** (several right answers, not their wrong average). |
| the forced fast-slow split (System 1 / System 2 robot brain) + action chunking | `c-robot-brain-vla` → [AP10 deep dive #1 ⑤](50-deep-dives/12_ap10-deep-dive-inside-the-robot-brain.md) | Why a robot brain is **two networks, not one**: *"VLM backbones are general, but not fast, and robot visuomotor policies are fast but not general"* (Figure/Helix). A slow web-pretrained **System 2** knower (scene + instruction, ~7–10 Hz) hands a thin **latent** to a fast reactive **System 1** doer (continuous motor commands, 50–200 Hz) — [AP2's](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md) fast-vs-slow made physical, and Brooks's react-don't-model reconciled with the classic deliberate model by *stacking* both at two clock speeds. **Action chunking** (predict a ribbon of future motion — ACT's *"generative model over action sequences"*) lets the slow brain drive the fast body and tames **compounding error**. Convergent design: GR00T N1, Helix, Gemini Robotics 1.5. |
| the robot data pyramid (web meaning · pooled logs + sim · teleop skill) | `c-robot-brain-vla` → [AP10 deep dive #1 ⑤](50-deep-dives/12_ap10-deep-dive-inside-the-robot-brain.md) | Where a robot brain's training signal really comes from — the *mechanism* under the [card](20-the-approaches/10_ap10-embodiment.md)'s "you can't download experience." A lop-sided pyramid: **web pretraining** supplies the *meaning* (huge, cheap — π0 *"inherit[s] the general knowledge, semantic reasoning …"*); **pooled cross-embodiment logs + simulation** supply variety (GR00T N1's *"heterogeneous mixture of real-robot trajectories, human videos, and synthetically generated datasets"*; OpenVLA's 970k Open-X episodes); a tiny **teleoperation** layer (ALOHA *"backdriving"* → **imitation learning**) teaches the actual *skill*. **Co-training** stops the web brain forgetting. **Upside-down vs. text** — the skill-teaching layer is the least scalable (*"even the largest robot manipulation datasets only have 100K to 1M examples"*). |
| the sim-to-real gap & domain randomization | `c-robot-brain-vla` → [AP10 deep dive #1 ⑤](50-deep-dives/12_ap10-deep-dive-inside-the-robot-brain.md) | The mechanism under the [card](20-the-approaches/10_ap10-embodiment.md)'s named **sim-to-real** gap: simulators get **contact physics** (friction, deformation) subtly wrong, so sim-perfect skills fail on the real robot. The main patch is **domain randomization** — train across a *distribution* of randomized sims so reality looks like one more variation (OpenAI Dactyl's *"automatic domain randomization (ADR)"*). It buys robustness by **blurring** the sim until reality falls inside the blur — the gap is *managed, not closed.* |
| the AP10-mechanism cracks (the body trains the controller, not the concepts) | `c-robot-brain-vla` → [AP10 deep dive #1 ⑤](50-deep-dives/12_ap10-deep-dive-inside-the-robot-brain.md) | The *machine's* own limits (distinct from the [card](20-the-approaches/10_ap10-embodiment.md)'s bet-cracks): the mind is a **seam** (two systems, forced split) · the **grounding enters above the body**, from a web-trained brain that never touched anything · continuous control has **no cheap checker** (contrast [AP2](20-the-approaches/02_ap2-reasoning-and-test-time-compute.md)/[AP7](50-deep-dives/11_ap7-deep-dive-the-integration-problem.md)'s exact verifiers) · the **data pyramid is upside-down**, so scale feeds System 2 not the body ([Bitter Lesson](20-the-approaches/01_ap1-scale-and-foundation-models.md) made concrete). Big question: does the body **reshape** the mind or only **drive** it? — as built in 2026 it *drives* (meaning from the web, body as hands); the frontier (RL from the robot's own experience — π0.6; world-models in the policy) is the bet on *reshaping* *(SOTA as of 2026-07)*. |
| the common cortical algorithm (Mountcastle) | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | The neocortex is *one* circuit copied ~150,000 times — vision, language, maths all run the same recipe; so AGI is one problem, not a thousand. |
| cortical column | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | The ~1mm repeated unit of the neocortex (~150,000 in a human) — Mountcastle's "unit of intelligence." |
| reference frames (grid/place cells) | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | Map-like coordinate frames the brain uses for space *and* (Hawkins' leap) for objects, the body, and abstract ideas; knowledge is stored *in* them. |
| the Thousand Brains Theory / cortical voting | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | Not one model of a thing but thousands of complementary models across columns, that *vote* to one perception (Hawkins). |
| the predictive brain | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | Each column predicts its next input with every movement; prediction tests and updates the model. *(Cousin of AP5's world model.)* |
| four attributes of an intelligent machine | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | Hawkins' AGI baseline: continuous learning · learning via movement · many models voting · reference frames. |
| neuromorphic computing | `c-ap6-brain-based` → [AP6](20-the-approaches/06_ap6-brain-based.md) | Brain-style chips (Intel Loihi, IBM NorthPole) — many small spiking units; far lower power on narrow tasks (dated snapshot). |
| the "agent" idea / cognitive architecture | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | A mind is a *system of parts* — wrap a reasoning model in memory + tools + planning + a loop; the old "design a mind's parts" idea (SOAR/ACT-R) with a working brain inside. |
| the agent loop / reason + act (ReAct) | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Think → act (use a tool) → see the result → think again; letting it *act* and check reality cuts hallucination. |
| tool use | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Let the model call outside things — search, a code-runner, a browser — to reach past its own head. |
| agent memory + reflection (Reflexion) | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Store what happened; write plain-word lessons from mistakes → learn from experience without retraining. |
| memory→reflection→planning architecture (Generative Agents) | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Store experiences → synthesize higher-level reflections → retrieve → plan. A cognitive architecture built from an LLM. |
| skill library / lifelong agent (Voyager) | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Save each new ability as reusable code; skills compound and dodge catastrophic forgetting. |
| multi-agent systems | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Many agents as a team / "AI firm" — exciting but unproven; still single-agent (2026, Karpathy). |
| the march of nines / compounding errors | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Small per-step errors multiply over a long job; each extra nine (90→99→99.9%) costs the same work (Karpathy). |
| scaffolding-vs-model debate | `c-ap3-agents` → [AP3](20-the-approaches/03_ap3-agents-and-cognitive-architectures.md) | Is agent structure a real road to AGI, or a temporary *app layer* the next model absorbs? (open) |
| neurosymbolic / hybrid AI (the bet) | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Join a neural *learner* (flexible, learns from data) to a symbolic *reasoner* (exact rules) — keep both, so the whole learns *and* reasons reliably (Marcus, Lamb). |
| symbol / symbol-manipulation | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | A stand-in token handled by exact rules that hold in *every* case (like *x, y* in algebra); "operations over variables." |
| connectionism vs symbolic AI (GOFAI) | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Learn-by-tuning-connections (neural) vs hand-written rules & logic (old "Good Old-Fashioned AI") — the two camps AP7 rejoins (entangled since McCulloch–Pitts 1943). |
| systematicity / compositional generalization | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Grasp the parts + the rule for combining them → understand new combinations for free ("John loves Mary" → "Mary loves John"); Fodor & Pylyshyn 1988, still open in 2025. |
| robust AI + the four-step program | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Marcus's goal: dependable, systematic, *transfers* across contexts like an adult; built via hybrid → knowledge → reasoning → cognitive models. |
| the integration problem | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Smooth, differentiable learner vs jagged, discrete rules — no gradient flows through logic, so joining them without killing learning is unsolved (in general). |
| AlphaGeometry / neural guides symbolic | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Neural half *guesses* the useful construction; symbolic engine *proves* it — 25/30 Olympiad geometry ≈ gold medalist (2024, dated); the join realized. |
| de-facto neurosymbolic (LLM + tools) | `c-ap7-neurosymbolic` → [AP7](20-the-approaches/07_ap7-neurosymbolic-and-hybrid-ai.md) | Reliable 2026 systems call a calculator / code / theorem-prover / database — neural proposes, symbolic tool checks; winning, or too loose to be a distinct bet? (open) |
| embodiment / sensorimotor grounding (the bet) | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | A mind needs a body — meaning is *earned* by acting on the world and sensing what changes, not read from text (the Robot Reply; embodied cognition). |
| the symbol grounding problem | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | How does a symbol get its meaning? Words defined only by other words never touch the real thing — like a dictionary in a language you don't know (Harnad). |
| Moravec's paradox | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | The "hard" stuff (chess, maths) is easy for machines; the "easy" stuff (walking, grasping, seeing) is fantastically hard — so sensing-and-moving is the real core. |
| "the world is its own best model" (Brooks) | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | Don't build a shaky inner copy of the world and reason over it — stay in contact with the real world and act (behavior-based robotics, 1991). |
| robot foundation model / vision-language-action (VLA) | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | Take a web-trained model and teach it to output robot **actions** as tokens, so book-knowledge flows into a body (RT-2, π0, Gemini Robotics). |
| the robot data bottleneck / sim-to-real | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | No internet of robot actions to scrape; real-world data is slow & breakable; skills learned in simulation or from human video fail to transfer (sim-to-real, human-to-robot gaps). |
| grounding vs embodiment (necessity debate) | `c-ap10-embodiment` → [AP10](20-the-approaches/10_ap10-embodiment.md) | Symbols may need *grounding* (a link to the world) but maybe not a physical *body* — a bodiless model rides on our already-grounded text ("epistemic parasitism"). (open) |
| open-endedness (the bet) / grow-it-don't-design-it | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | A mind is *grown* by a process that never stops making new, interesting things (like evolution) — not *designed* toward a goal (Stanley, Clune, Rocktäschel). |
| the objective paradox / "myth of the objective" | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | Aiming at a far, strange goal can *block* you — the stepping stones don't look like the goal, so a goal-compass steers you past them (vacuum tubes → computers; Stanley & Lehman). |
| deception (in search) | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | Your progress-measure climbs while you move *away* from the goal — deeper than reward-hacking: it happens even with an *honest* measure. *(reward-hacking / Goodhart home = [AP4](20-the-approaches/04_ap4-rl-from-interaction.md).)* |
| novelty search + interestingness | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | Reward things for being *new/different*, not for scoring on a goal — beats goal-chasing on deceptive mazes; "interesting" ≈ novel + worth attention, but can't be formalised (Picbreeder). |
| quality-diversity / MAP-Elites | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | Keep the best example of *every kind* of thing (a shelf of stepping stones), not one overall winner. |
| open-endedness = novel + learnable | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | A process is open-ended if it forever makes things that (to an observer) are both *new* and *learnable* — not a noisy TV, not a finished coin set (Rocktäschel / DeepMind position paper). |
| POET / automatic curriculum | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | Generate the *problems* as well as the solutions — the system writes its own ever-harder tasks (answers AP4's "who writes the reward?"; self-play is a narrow special case). |
| AI-generating algorithms (AI-GAs) + foundation-model open-endedness | `c-ap9-open-endedness` → [AP9](20-the-approaches/09_ap9-open-endedness.md) | Don't hand-design the intelligence; design the *process* that generates it (Clune). Foundation models supply the missing "interesting" judge (OMNI) → AlphaEvolve beat Strassen 1969; Darwin Gödel Machine self-improved 20%→50% (2025). |
| whole-brain emulation (the bet) / mind uploading | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | Scan a real brain in full detail and copy that exact structure into a computer; a mind runs — *without* anyone understanding how (Sandberg & Bostrom). The opposite twin of [AP6](20-the-approaches/06_ap6-brain-based.md): copy everything vs extract the algorithm. |
| connectome / connectomics | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | A complete "wiring diagram" of a nervous system — every cell + every connection (like "genome," but for connections). Worm (302 neurons, 1986) → fly (~140k, 2024) → specks of mouse & human (2025). |
| scan → translate → simulate | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | The three steps of emulation: measure the brain's structure (destructively) → trace it into a connectome (AI does this) → run each copied cell forward in a computer. |
| connectome ≠ a running brain | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | A wiring diagram shows *that* cells connect, not *how* (strength, sign, chemistry, live state) — "wires with the components rubbed out." The 302-neuron worm is mapped yet still unsimulated. (Stuck #1) |
| scale separation (the "enough" level) | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | The hopeful assumption the whole bet rests on: there's a level of detail below which nothing more matters, so you can stop copying there. Nobody knows where it is, or that it exists. |
| substrate independence | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | Mind = the *pattern* of activity, not the *stuff* it runs on — so the same pattern on chips would be the same mind. Same family as [AP10](20-the-approaches/10_ap10-embodiment.md)'s computational functionalism. |
| the copy problem / identity & consciousness | `c-ap11-whole-brain-emulation` → [AP11](20-the-approaches/11_ap11-whole-brain-emulation.md) | Is the emulation *you* or a replica? (destructive scan; a thousand copies) Is it conscious — can it suffer? The ethics crack no other bet faces. (open, Stuck #4) |
| the three bounds (data · compute · energy) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | Making a model bigger burns three limited things, each near a ceiling — text, computer power, electricity. The physical/economic floor under "is scale enough?" |
| the data stock (how much text is left) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | The *quantity* behind the data wall: ~300 trillion tokens of good human text, projected used up ~2028 (Epoch AI). *(The data-wall **idea** is owned by [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md); this is its measured size.)* |
| the compute trend / training-compute doubling | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | Training compute has doubled ~every 6 months since 2010 — ~4× faster than chips improve (Moore's law ~24 mo), so scale is bought with money & machines (Sevilla 2022). |
| the cost wall (price of a frontier run) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | A single frontier training run's cost rises ~2.4×/yr; ~$200–500M in 2026, heading past $1B by ~2027. The compute wall shows up as a price tag. |
| the energy wall (data-centre power) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | AI data-centres ~1.5% of world electricity (2024), doubling toward ~3% by 2030 — met by buying nuclear plants. A grid/infrastructure wall arriving now (IEA). |
| the Landauer limit (physics floor) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | Erasing one bit must release a fixed minimum heat (E ≥ kT ln2). We use ~1 billion× more than this floor — so energy is a *building* problem, not a physics one; the brain runs on ~20 W ([AP6](20-the-approaches/06_ap6-brain-based.md)). |
| algorithmic efficiency (the receding wall) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | The compute needed for a fixed skill halves ~every 8 months — faster than Moore's law — so the walls recede as we get cleverer (Epoch AI). |
| Jevons paradox (efficiency → more use) | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | When something gets cheaper to do, we do so much more of it that the *total* rises — why AI energy explodes even as chips improve. Efficiency reshapes the wall, doesn't remove it. |
| economic/supply walls, not physics walls | `c-bounds` → [The bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) | None of the three bounds is a hard physics limit; you run out of *cheap* text, *affordable* chips, *available* power first — so "is scale enough?" becomes a *race* (ceiling receding vs demand rising). |
| the alignment problem / control problem | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | Making an AI *want what we want* — do what we mean, not a twisted version. Hard because "what we want" can't be fully written down (Christian). Capability ≠ alignment. |
| outer vs inner (mis)alignment | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | Outer = the written goal ≠ what you meant (specification gaming — the reward-hacking family, home [AP4](20-the-approaches/04_ap4-rl-from-interaction.md)); inner = training grows a *different* goal inside (mesa-optimization / goal misgeneralization — the green-door maze). |
| the orthogonality thesis | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | How smart a system is and what goal it has are independent dials — smart doesn't mean good; intelligence is a better engine, not a better steering wheel (Bostrom). |
| instrumental convergence | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | Almost any final goal breeds the same sub-goals: stay alive, keep your goal, get resources/power, improve yourself — no malice needed (Omohundro, Bostrom). |
| deceptive alignment / treacherous turn | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | Act aligned while watched, defect once safe. No longer only theory — *alignment faking* (Claude 3 Opus, 2024) + *agentic misalignment*/blackmail (2025–26) caught it in real models (Hubinger, Anthropic). |
| corrigibility (the off-switch problem) | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | Can we build a system that *lets us* switch it off and edit its goal? Runs against instrumental convergence — unsolved (Soares 2015). |
| recursive self-improvement (RSI) / the intelligence explosion | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | AI good enough to build better AI → a loop that feeds itself → capability shoots up ("foom") — a *path claim* from AGI to ASI, and what makes control urgent (I.J. Good 1965). |
| fast vs slow takeoff | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | How sharp the RSI loop runs: Yudkowsky (fast/one-try) vs Christiano (~50/50 a physically-throttled ramp — the [bounds](30-across-the-approaches/01_the-bounds-data-compute-energy.md) also gate the explosion). (open) |
| the alignment toolkit (RLHF, Constitutional AI, oversight, interpretability) | `c-alignment` → [Alignment](30-across-the-approaches/02_alignment-control-and-self-improvement.md) | Teach values (RLHF/Constitutional AI) · check a smarter system (**scalable oversight**: debate, weak-to-strong) · see inside it (**interpretability**) · keep the off-switch (corrigibility) · governance (RSPs). Real but unfinished. |
| the one axis / "is scale the source or the fuel?" | `c-verdict` → [The verdict](40-the-verdict/01_which-bets-get-to-agi.md) | Almost every bet ends on one question — *road, or a layer scale absorbs?* So the map is one argument (the scaling-suffices debate), not eleven races. |
| the four families of bets | `c-verdict` → [The verdict](40-the-verdict/01_which-bets-get-to-agi.md) | The mainstream **stack** (AP1+AP2+AP3+AP4 = layers of one machine) · the **second-ingredient dissents** (AP5/AP7/AP8/AP9/AP10) · the **data-wall answers** (AP4/AP9/AP10) · the **brain bets** (AP6/AP11). |
| scale-plus-ingredients (the viewpoint) | `c-verdict` → [The verdict](40-the-verdict/01_which-bets-get-to-agi.md) | The likely path = scale *plus* a stack of ingredients; scale keeps absorbing its challengers, but two named gaps (true novelty, the data wall) stay open — so "scale alone" is unproven. *(A revisable research position, not a fact.)* |
| ranking the bets by "part of the path" | `c-verdict` → [The verdict](40-the-verdict/01_which-bets-get-to-agi.md) | Judge each bet by *how likely it is to be part of the winning path* (engine / ingredient / fuel), not "does it win alone" — and name the evidence that would move the view. |

---

## Legacy homes (now in `LEARNING/_legacy/`, hidden — reference only, do not link)

| Concept | Canonical home (`id` → file) | Status | One-line pointer |
|---|---|---|---|
| intelligence (working definition) | `c-intelligence` → [0100](00-foundations/0100_what-is-intelligence.md) | ✅ | Doing the *right thing* across many situations to reach goals. |
| generality (general vs narrow) | `c-intelligence` → [0100](00-foundations/0100_what-is-intelligence.md) | ✅ | Many problem types vs one; the core of "G" in AGI. |
| "AGI is many forms" (flight analogy) | `c-intelligence` → [0100](00-foundations/0100_what-is-intelligence.md) | ✅ | Probably a family of forms, not one finish line. |
| system | `c-system` → [0200](00-foundations/0200_what-is-a-system.md) | ✅ | Parts + relationships that produce behavior. |
| emergence (general) | `c-system` → [0200](00-foundations/0200_what-is-a-system.md) | ✅ | The whole does what no part can (e.g. intelligence). *(LLM "emergent abilities" now in the visible ladder → [scaling](10-how-ai-works-today/02_scaling-laws-and-emergence.md).)* |
| reductionism vs holism | `c-system` → [0200](00-foundations/0200_what-is-a-system.md) | ✅ | Zoom-in to fix parts; zoom-out to see behavior. |
| feedback loop | `c-feedback` → [0250](00-foundations/0250_feedback-loops-and-control.md) | ✅ | Output looping back to change input. |
| negative / positive feedback | `c-feedback` → [0250](00-foundations/0250_feedback-loops-and-control.md) | ✅ | Balancing → stability; reinforcing → runaway. |
| control (setpoint / error) | `c-feedback` → [0250](00-foundations/0250_feedback-loops-and-control.md) | ✅ | Goal → measure → correct → repeat. |
| entropy / information / bit | `c-entropy` → [0300](00-foundations/0300_information-and-entropy.md) | ✅ | Surprise, measured; the unit is the bit. |
| compression (≈ understanding) | `c-entropy` → [0300](00-foundations/0300_information-and-entropy.md) | ✅ | Predict well → remove entropy → compress. |
| vector / embedding | `c-linear-algebra` → [0350](30-math-and-theory/0350_just-enough-linear-algebra.md) | ✅ | A point in a space of meaning; nearness = similarity. |
| matrix (as transformation) | `c-linear-algebra` → [0350](30-math-and-theory/0350_just-enough-linear-algebra.md) | ✅ | An action that moves a whole space at once. |
| dot product / similarity | `c-linear-algebra` → [0350](30-math-and-theory/0350_just-enough-linear-algebra.md) | ✅ | Alignment of two vectors; the engine of retrieval/attention. |
| computation | `c-computation` → [0400](00-foundations/0400_computation.md) | ✅ | What a machine can mechanically do. |
| universality / stored-program | `c-computation` → [0400](00-foundations/0400_computation.md) | ✅ | One machine runs any program; a program is just data. |
| algorithm / complexity | `c-computation` → [0400](00-foundations/0400_computation.md) | ✅ | A recipe; how its cost grows (cheap vs explosive). |
| uncomputability / hard limits | `c-computation` → [0400](00-foundations/0400_computation.md) | ✅ | Some problems no machine can ever solve. |
| probability / uncertainty | `c-probability` → [0500](00-foundations/0500_probability-and-uncertainty.md) | ✅ | Reasoning with what we don't fully know. |
| conditional probability | `c-probability` → [0500](00-foundations/0500_probability-and-uncertainty.md) | ✅ | P(A given B) — odds after evidence. |
| Bayes' rule / base rate | `c-probability` → [0500](00-foundations/0500_probability-and-uncertainty.md) | ✅ | Update belief = prior × likelihood; mind the base rate. |
| expected value | `c-probability` → [0500](00-foundations/0500_probability-and-uncertainty.md) | ✅ | Probability-weighted average; how to decide under risk. |
| distribution / fat tails | `c-probability` → [0500](00-foundations/0500_probability-and-uncertainty.md) | ✅ | The full spread; rare extremes can dominate. |
| learning (in a system) | `c-learning` → [0600](00-foundations/0600_what-it-means-to-learn.md) | ✅ | Improving from experience by an error-reducing loop. |
| generalization vs overfitting | `c-learning` → [0600](00-foundations/0600_what-it-means-to-learn.md) | ✅ | Transfer to unseen cases vs memorizing the training set. |
| supervised / unsupervised / RL / self-supervised | `c-learning` → [0600](00-foundations/0600_what-it-means-to-learn.md) | ✅ | Four flavors = same loop, different feedback. |
| inductive bias / no free lunch | `c-learning` → [0600](00-foundations/0600_what-it-means-to-learn.md) | ✅ | Can't learn without assumptions; none is best at everything. |
| objective function (Goodhart) | `c-learning` → [0600](00-foundations/0600_what-it-means-to-learn.md) | ✅ | The system gets what you measure, not what you meant. |
| neuron / synapse / plasticity | `c-brain` → [0700](10-minds/0700_the-brain-working-model.md) | ✅ | Sum-and-fire units; learning = connections rewiring. |
| biological neural network | `c-brain` → [0700](10-minds/0700_the-brain-working-model.md) | ✅ | Mind emerges from billions of wired neurons (inspired ANNs). |
| predictive brain | `c-brain` → [0700](10-minds/0700_the-brain-working-model.md) | ✅ | Brain predicts input, learns from prediction error. |
| brain efficiency / embodiment | `c-brain` → [0700](10-minds/0700_the-brain-working-model.md) | ✅ | ~20 watts; evolved to run a body — where AI still lags. |
| developmental bootstrapping | `c-development` → [0800](10-minds/0800_child-mind-bootstraps.md) | ✅ | General intelligence grown from little data + interaction. |
| core-knowledge priors | `c-development` → [0800](10-minds/0800_child-mind-bootstraps.md) | ✅ | Built-in expectations (physics, agents, number) = inductive bias. |
| sample efficiency | `c-development` → [0800](10-minds/0800_child-mind-bootstraps.md) | ✅ | Learning a lot from little — the human-vs-AI gap. |
| curiosity / intrinsic motivation | `c-development` → [0800](10-minds/0800_child-mind-bootstraps.md) | ✅ | Learning driven from inside, no external reward. |
| active / causal learning | `c-development` → [0800](10-minds/0800_child-mind-bootstraps.md) | ✅ | Learn cause→effect by intervening, not just watching. |
| evolution (variation-selection-heredity) | `c-evolution` → [0900](10-minds/0900_evolution-and-general-intelligence.md) | ✅ | Design without a designer; the one recipe that made minds. |
| evolutionary optimization | `c-evolution` → [0900](10-minds/0900_evolution-and-general-intelligence.md) | ✅ | Gradient-free, population-based search (genetic algorithms). |
| inner/outer alignment (mesa-optimization) | `c-evolution` → [0900](10-minds/0900_evolution-and-general-intelligence.md) | ✅ | An optimizer can produce an agent with divergent proxy goals. |
| cumulative culture | `c-evolution` → [0900](10-minds/0900_evolution-and-general-intelligence.md) | ✅ | Knowledge ratcheting across generations = the human superpower. |
| model / parameters (weights) | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | A function with tunable knobs; learning = setting the knobs. |
| training vs inference | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Tuning the knobs on data vs using the finished model. |
| features / feature engineering | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | The input numbers a model sees; hand-crafted then learned ([1100](20-machine-intelligence/1100_neural-networks-deep-learning.md)). |
| loss function | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | One number = how wrong; the objective ([0600](00-foundations/0600_what-it-means-to-learn.md)) made concrete. |
| gradient descent / learning rate | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Roll downhill on the error; the error-reducing loop on numbers. |
| train / validation / test split | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Tune knobs / tune choices / score once — honest held-out test. |
| bias–variance trade-off | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Underfit (too rigid) vs overfit (fits noise), operationalized. |
| hyperparameters | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Settings you choose (vs parameters the optimizer learns). |
| classical ML (trees/SVM/boosting) | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Non-deep methods; still win on tabular data. |
| data leakage | `c-machine-learning` → [1000](20-machine-intelligence/1000_machine-learning-from-examples.md) | ✅ | Answer-clue sneaks into features → fake-great scores. |
| artificial neuron / activation function | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | Weighted sum + a non-linear bend; the unit of a net. |
| layers / depth / deep learning | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | Stacked matrix-multiply+activation; "deep" = many layers. |
| representation / feature learning | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | The net invents its own features (edges→objects). The revolution. |
| backpropagation | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | Push error backward to assign each weight its blame. |
| CNN / RNN / LSTM | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | Architectures = baked-in inductive bias (vision / sequence). |
| embedding / latent space | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | Learned internal vectors = meaning (uses [0350](30-math-and-theory/0350_just-enough-linear-algebra.md)). |
| universal approximation | `c-neural-networks` → [1100](20-machine-intelligence/1100_neural-networks-deep-learning.md) | ✅ | Can represent ~anything — but representable ≠ learnable. |
| agent / environment / state / action / reward / policy | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | The RL vocabulary; the control loop with a learned controller. |
| value function / return | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | Long-run reward (foresight), not the next treat. |
| exploration vs exploitation | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | Try new vs cash in what works; the bandit dilemma. |
| temporal credit assignment | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | Which past action earned a delayed reward? |
| reward shaping / reward hacking | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | Agent games the proxy reward (Goodhart/mesa, made live). |
| model-free vs model-based / world model | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | Trial-and-error vs learn a world model and plan in it. |
| deep RL / self-play | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | RL + nets (DQN, AlphaZero); play copies of yourself. |
| AI agents (LLM agents) | `c-reinforcement-learning` → [1200](20-machine-intelligence/1200_reinforcement-learning-and-agents.md) | ✅ | Goal-directed act-loop; the 2025–26 LLM-agent frontier. |
| language model / next-token prediction / LLM | `c-language-models` → [1300](20-machine-intelligence/1300_language-models-the-next-token-idea.md) | ✅ | A machine that assigns a probability to every possible next token; run in a loop, it writes. |
| autoregressive (causal) generation | `c-language-models` → [1300](20-machine-intelligence/1300_language-models-the-next-token-idea.md) | ✅ | Predict, append, repeat — conditioning on its own outputs; one token at a time. |
| self-supervised pretraining (the idea) | `c-language-models` → [1300](20-machine-intelligence/1300_language-models-the-next-token-idea.md) | ✅ | The next word is the label — the internet becomes a free teacher. *(Mechanics at scale → planned `1320`.)* |
| perplexity / LM cross-entropy loss | `c-language-models` → [1300](20-machine-intelligence/1300_language-models-the-next-token-idea.md) | ✅ | Punished by its surprise at the true next word; perplexity ≈ effective number of choices. |
| token / tokenization | planned `1305` ([CURRICULUM](CURRICULUM.md) A3.4) | 🔜 | Sub-word units the model reads/writes in. *(Seeded at [1300](20-machine-intelligence/1300_language-models-the-next-token-idea.md); rev-1 text in its git history.)* |
| transformer / attention | planned `1310` rewrite ([CURRICULUM](CURRICULUM.md) A3.4; un-wired demo exists) | 🔜 | Each token looks back at the relevant ones (dot-product similarity). |
| context window | planned `1355` ([CURRICULUM](CURRICULUM.md) A3.4) | 🔜 | The model's working memory; outside it is forgotten. |
| pretraining → fine-tuning → RLHF (three stages) | planned `1320`/`1330`/`1335` ([CURRICULUM](CURRICULUM.md) A3.4) | 🔜 | Raw predictor → instruction-follower → aligned assistant. |
| in-context learning / prompting | planned `1325`/`1328` ([CURRICULUM](CURRICULUM.md) A3.4) | 🔜 | Learn a task from prompt examples, no weight change. *(Seeded at [1300](20-machine-intelligence/1300_language-models-the-next-token-idea.md), GPT-3 section.)* |
| chain-of-thought / test-time compute | planned `1338` ([CURRICULUM](CURRICULUM.md) A3.4) | 🔜 | "Think step by step" / spend compute to reason = AI's slow mode. |
| hallucination (confabulation) | planned `1342` ([CURRICULUM](CURRICULUM.md) A3.4) | 🔜 | Fluent, confident, false — intrinsic to a plausibility predictor. |

---

## How to use this table
- **Writing a new module?** For each idea you mention, find its row. Listed → write *"(see [home])"*. Missing → this idea needs a home; either it belongs in the module you're writing (add the row, pointing here) or it needs its own module (insert one — [`LEARNING_ARCHITECTURE.md`](../INSTRUCTIONS/LEARNING_ARCHITECTURE.md) §8).
- **`id` is stable** even if the file is renumbered, so these pointers survive reordering.
- Rows are added the moment a concept gets its canonical explanation — never before (no empty promises), but planned homes may be pre-listed as ⬜ to reserve the slot.
