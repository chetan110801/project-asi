# RESOURCES/library/ — local only (not published)

Drop **purchased / copyrighted** source files here: PDFs, EPUBs, textbooks, slide decks (PPT/PDF), etc.

- This folder's **contents are git-ignored** — they stay on your machine and are **never pushed** to the public GitHub repo (copyright). Only this README is tracked.
- I (the AI) read files here to write your learning modules, citing them by their catalog `id`.
- **Naming tip:** `r-id__short-title.ext` — e.g., `r-kandel__principles-neural-science.pdf`. The `id` matches a row in [`../INDEX.md`](../INDEX.md) so notes link back to the source.
- See [`../REQUESTS.md`](../REQUESTS.md) for the prioritized list of what to obtain.
